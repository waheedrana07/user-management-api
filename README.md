# User Management API

A RESTful API built with **Node.js**, **Express**, and **MongoDB Atlas**. This project handles user data and includes basic authentication security.

## 🚀 Features
- **Create Users:** POST request to save users to MongoDB.
- **Security:** Basic Authentication implemented for sensitive routes.
- **Database:** Fully integrated with MongoDB Atlas Cloud.

## 🛠️ Tech Stack
- **Backend:** Node.js, Express.js
- **Database:** MongoDB (Mongoose ODM)
- **Testing:** REST Client / Postman

## 🚦 How to Run
1. Clone the repo: `git clone https://github.com/waheedrana07/user-management-api.git`
2. Install dependencies: `npm install`
3. Create a `.env` file with your `MONGO_URI`.
4. Run the server: `node index.js`